---
- hosts: 172.31.1.220
  become: yes
  tasks:
  - include: myfile.yml
  - include: myfile2.yml

  - name: install webserver
    apt:
     name: apache2
     state: present 
  - name: copy index file into node side
    copy:
      src: myindexpage
      dest: /var/www/html/index.html   